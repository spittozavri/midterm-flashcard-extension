{
  "name": "midterm-flashcard-extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite preview --outDir dist --open",
    "build": "tsc && vite build && node postbuild.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.317",
    "@types/node": "^22.15.2",
    "typescript": "~5.7.2",
    "vite": "^6.3.3",
    "vitest": "^3.1.2"
  }
}
